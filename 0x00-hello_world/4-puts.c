#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = puts("\"Programming is like building a multilingual puzzle");

	if (n == EOF)
		perror("puts()");

	return (0);
}
